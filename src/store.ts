import Vue from "vue";
import Vuex from "vuex";

// Use Vuex
Vue.use(Vuex);

/**
 * Store
 */
export default new Vuex.Store({
    // Application State
    state: {
    },

    // Synchronous methods to set the state values
    mutations: {
    },

    // Asynchronous methods to set the state values
    actions: {
    },

    // Methods for returning values that require additional code logic
    getters: {}
});